&НаКлиенте
Процедура Войти(Команда)
	
	Если ПустаяСтрока(Логин) Тогда
		Возврат;
	КонецЕсли;
	АутентификацияИАвторизация.ВыполнитьАвторизацию(ЭтаФорма.Логин);
	
	ЗавершитьРаботуСистемы(Ложь, Истина);
	
КонецПроцедуры
